package Lista02Exercicios;

import java.util.Scanner;

public class Exe02 {
	public static void main(String args[]) {
		
		// 2- Faça um programa que entre com três números e coloque em ordem crescente.
		
		Scanner ler = new Scanner(System.in);
		int n1, n2, n3;
			
		System.out.print("Digite um valor qualquer na variavel 'n1' :");
		n1 = ler.nextInt();
		System.out.print("Digite um valor qualquer na variavel 'n2' :");
		n2 = ler.nextInt();
		System.out.print("Digite um valor qualquer na variavel 'n3' :");
		n3 = ler.nextInt();
		
		if( (n1>=n2) && (n2>=n3) ) {
			System.out.printf("ordem crescente:%d %s %d %s %d",n1,", ",n2,", ",n3);
		}
		else if( (n1>=n3) && (n3>n2) ) {
			System.out.printf("ordem crescente:%d %s %d %s %d",n1,", ",n3,", ",n2);
		}
		else if( (n2>=n1) && (n1>=n3) ) {
			System.out.printf("ordem crescente:%d %s %d %s %d",n2,", ",n1,", ",n3);
		}
		else if( (n2>=n3) && (n3>n1) ) {
			System.out.printf("ordem crescente:%d %s %d %s %d",n2,", ",n3,", ",n1);
		}
		else if( (n3>=n1) && (n1>=n2) ) {
			System.out.printf("ordem crescente:%d %s %d %s %d",n3,", ",n1,", ",n2);
		}
		else if( (n3>=n2) && (n2>n1) ) {
			System.out.printf("ordem crescente:%d %s %d %s %d",n3,", ",n2,", ",n1);
		}
	}
}
